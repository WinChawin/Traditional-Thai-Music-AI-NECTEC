[project]
name = "finding-instrument"
version = "0.1.0"
description = "Thai classical music instrument classification from symbolic notation"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "B"]
